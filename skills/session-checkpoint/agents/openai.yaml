interface:
  display_name: "Session checkpoint"
  short_description: "Save this session to ./sessions/ for another machine"
  default_prompt: "Write a checkpoint for this session."
policy:
  allow_implicit_invocation: false
