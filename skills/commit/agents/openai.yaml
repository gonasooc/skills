interface:
  display_name: "Commit"
  short_description: "Commit in the convention the repo already uses"
  default_prompt: "Use $commit to commit my changes."
policy:
  allow_implicit_invocation: true
