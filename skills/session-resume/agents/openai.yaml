interface:
  display_name: "Session Resume"
  short_description: "Verify and resume a saved repository session"
  default_prompt: "Use $session-resume to verify the latest checkpoint before continuing."
policy:
  allow_implicit_invocation: false
